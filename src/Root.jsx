import React, { Component } from 'react';
import { Router } from'react-router';

class Root extends Component {
  render() {
    return (
      <Router>
        
      </Router>
    );
  }
}

export default Root;
